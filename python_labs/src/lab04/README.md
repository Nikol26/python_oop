# Лабораторная работа №4 — Интерфейсы и абстрактные классы (ABC)

## 1. Цель работы

Познакомиться с абстрактными базовыми классами ABC, изучить понятие интерфейса как контракта поведения и научиться задавать обязательные методы для классов.

В данной лабораторной работе закрепляется полиморфизм через единый интерфейс и проектирование архитектуры программы, где разные объекты могут обрабатываться одинаково.
## 2. Описание интерфейсов

В лабораторной работе созданы два интерфейса: Printable и Comparable.

### Интерфейс Printable

Интерфейс Printable задаёт общий контракт для объектов, которые можно вывести в текстовом виде.

Обязательный метод:

- to_string() — возвращает строковое описание объекта.

Данный интерфейс используется для единого вывода информации о студентах разных типов.

### Интерфейс Comparable

Интерфейс Comparable задаёт общий контракт для объектов, которые можно сравнивать между собой.

Обязательный метод:

- compare_to(other) — сравнивает текущий объект с другим объектом.

Метод возвращает:

- 1, если текущий объект больше;
- -1, если текущий объект меньше;
- 0, если объекты равны.
 ## 3. Реализация в классах

Интерфейсы реализованы в следующих классах:

- PrintableBachelorStudent
- PrintableMasterStudent
- PrintablePhDStudent

Данные классы наследуются от классов из лабораторной работы №3 и дополнительно реализуют интерфейсы Printable и Comparable.

### Класс PrintableBachelorStudent

Реализует методы:

- to_string() — выводит информацию о бакалавре;
- compare_to(other) — сравнивает объекты по GPA.

Дополнительно выводятся:

- направление подготовки;
- форма обучения.

### Класс PrintableMasterStudent

Реализует методы:

- to_string() — выводит информацию о магистре;
- compare_to(other) — сравнивает объекты по GPA.

Дополнительно выводятся:

- программа обучения;
- область исследования.

### Класс PrintablePhDStudent

Реализует методы:

- to_string() — выводит информацию об аспиранте;
- compare_to(other) — сравнивает объекты по GPA.

Дополнительно выводятся:

- тема исследования;
- научный руководитель.

### Отличия поведения классов

Все классы имеют одинаковые методы интерфейсов, но выводят разную информацию в зависимости от типа студента.

Это демонстрирует полиморфизм: один и тот же метод работает по-разному у разных объектов.

В данной лабораторной работе сравнение выполняется по среднему баллу GPA.

## 4. Демонстрация работы

В файле demo.py реализовано 6 сценариев, демонстрирующих работу интерфейсов, абстрактных классов и полиморфизма.

### Сценарий 1 — Создание объектов и вызов интерфейсных методов

<img width="1394" height="194" alt="image" src="https://github.com/user-attachments/assets/dedbf81e-5a9a-4085-b506-10b9620c63fc" />


Создаются объекты классов:

- PrintableBachelorStudent
- PrintableMasterStudent
- PrintablePhDStudent

Для каждого объекта вызывается метод to_string().

Демонстрируется различный вывод информации для каждого типа студента.

### Сценарий 2 — Универсальная функция через интерфейс Printable

<img width="1321" height="200" alt="image" src="https://github.com/user-attachments/assets/616369b4-dc98-436d-b6a0-4899d676967c" />


Используется функция print_all(items: list[Printable]).

Функция принимает список разных объектов и вызывает у каждого метод to_string().

Это показывает работу через единый интерфейс.

### Сценарий 3 — Проверка через isinstance()

<img width="1339" height="425" alt="image" src="https://github.com/user-attachments/assets/d3ed8560-fddc-461a-8c70-696ff0064042" />


Для каждого объекта выполняется проверка реализации интерфейсов:

- Printable
- Comparable

Подтверждается реализация нескольких интерфейсов одним классом.

### Сценарий 4 — Сравнение объектов

<img width="1344" height="196" alt="image" src="https://github.com/user-attachments/assets/4d5a7886-85a8-4118-b2b0-c81c6bc60476" />


Используется метод compare_to(other).

Сравнение выполняется по значению GPA.

### Сценарий 5 — Коллекция объектов

<img width="1376" height="394" alt="image" src="https://github.com/user-attachments/assets/d32cc6af-c5d0-4122-b763-1b1cd4a69a0c" />



Создаётся коллекция InterfaceStudentCollection.

Демонстрируется:

- добавление объектов;
- хранение разных типов в одном списке;
- фильтрация по интерфейсам.

### Сценарий 6 — Сортировка через интерфейс Comparable

<img width="1342" height="254" alt="image" src="https://github.com/user-attachments/assets/024974ea-e5cc-4ce9-a52a-5751d55c415c" />


Коллекция сортирует студентов по GPA через метод compare_to(other).

Это демонстрирует использование интерфейса для архитектурного поведения программы.
## 5. Вывод

В ходе выполнения лабораторной работы №4 были изучены абстрактные базовые классы ABC и интерфейсы как способ задания обязательного поведения для классов.

Были созданы два интерфейса:

- Printable — для вывода объектов в текстовом виде;
- Comparable — для сравнения объектов между собой.

На практике было показано, что разные классы могут реализовывать одинаковые методы по-разному, что демонстрирует полиморфизм.

Также была реализована работа через единый интерфейс без использования большого количества условий, что делает программу более гибкой и удобной для расширения.

Дополнительно была выполнена интеграция интерфейсов с коллекцией объектов, реализована фильтрация и сортировка студентов по среднему баллу GPA.

В результате лабораторной работы были закреплены навыки проектирования архитектуры программы с использованием интерфейсов и абстрактных классов.


